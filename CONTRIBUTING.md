# Contributing to Takeoff Backend

Thank you for your interest in contributing to the Takeoff Backend! We welcome contributions from the community.

## 🛠️ Getting Started

1.  **Fork** the repository.
2.  **Clone** your fork:
    ```bash
    git clone https://github.com/YOUR_USERNAME/takeoff-backend.git
    cd takeoff-backend
    ```
3.  **Install dependencies**:
    ```bash
    npm install
    ```
4.  **Set up environment**:
    Copy `.env.example` (create one if missing) to `.env` and fill in required values.

## 🧪 Running Tests

Before submitting a Pull Request, please ensure all tests pass.

```bash
npm test
```

## 📝 Coding Standards

- **TypeScript**: We use TypeScript for type safety. Ensure no implicit `any` types.
- **Linting**: Run `npm run lint` to check for style issues.
- **Commits**: Use clear, descriptive commit messages.

## 🚀 Submitting a Pull Request

1.  Create a new branch for your feature or fix:
    ```bash
    git checkout -b feature/amazing-feature
    ```
2.  Commit your changes.
3.  Push to your fork.
4.  Open a Pull Request against the `main` branch of the original repository.

## 🐛 Reporting Issues

If you find a bug, please create an issue in the repository with a detailed description and steps to reproduce.
